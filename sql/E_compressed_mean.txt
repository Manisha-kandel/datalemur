-- E_compressed_mean, 20 minutes
WITH cte AS (
SELECT 
  SUM(order_occurrences*item_count) AS total_items , 
  SUM(order_occurrences) AS total_orders
FROM items_per_order
)

SELECT ROUND((total_items/total_orders)::NUMERIC, 1) AS mean
FROM cte 
;