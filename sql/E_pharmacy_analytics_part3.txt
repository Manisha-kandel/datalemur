-- E_pharmacy_analytics_part3, 10 minutes
SELECT 
  manufacturer, 
  '$' || ROUND(SUM(total_sales)/1000000, 0)::TEXT || ' million' AS sales_mil
FROM pharmacy_sales
GROUP BY manufacturer
ORDER BY 
  SUM(total_sales) DESC,
  manufacturer ASC
;