
WITH cte as 
(
SELECT user_id, COUNT(*) as num_of_tweets
FROM tweets
WHERE EXTRACT(YEAR FROM tweet_date) = 2022
GROUP BY user_id
)

SELECT num_of_tweets as tweet_bucket, COUNT(*) as users_num
FROM cte
GROUP BY num_of_tweets;









